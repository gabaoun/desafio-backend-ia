services:
  api:
    build: .
    container_name: backend_ai_api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn questao_1.main:app --host 0.0.0.0 --port 8000 --reload
